PI user extension installed at ~/.pi/agent/extensions/pi-acp-bridge/index.ts. It exposes a local-only ACP-compatible HTTP bridge over PI native extension/RPC APIs on 127.0.0.1:8765 by default, with flags --acp/--no-acp and --acp-port, commands /acp-status, /acp-agents, /acp-start, /acp-stop, /acp-port. Old experimental ~/.pi/agent/extensions/design-acp was removed. <!-- created=2026-06-06, last=2026-06-06 -->
§
Added Pi extension at agent/extensions/tool-call-guard/index.ts that detects assistant text containing literal <tool_call>/<function=...>/<parameter=...> tags without real tool calls, notifies the UI, and queues a follow-up user repair prompt instructing the model to retry with actual function calls. Toggle with /tool-call-guard [on|off]. <!-- created=2026-06-07, last=2026-06-07 -->
§
Pi extension tool renderers don't inherit custom design automatically. Tools registered by other extensions (context-mode, @feniix/bridgekit, pi-markdown-preview, pi-mcp-adapter) need renderers patched directly in their source files under agent/npm/node_modules/. Patched files: context-mode MCP bridge (mcp-bridge.js), bridgekit adapter (adapters/pi.js), pi-markdown-preview (index.ts), pi-mcp-adapter (index.ts + tool-result-renderer.ts). ctx_batch_execute renderer was patched to respect collapsed/expanded modes (collapsed shows line count only, expanded limits to 40 lines). <!-- created=2026-06-07, last=2026-06-07 -->
§
In ~/.pi, design-enhancer extension patches Pi InteractiveMode and overrides core tool renderers. For /new, /resume, or /fork stability, reinstall the startup-menu monkey patch on every extension runtime load (do not keep a stale prototype closure), clear stale ctxRef on session_shutdown, and keep renderers defensive against missing usage/content in old sessions. <!-- created=2026-06-09, last=2026-06-09 -->
§
Pi custom config backup repo lives in `/home/ncls/.pi` with remote `git@github.com:ncls-p/pi-agent-config.git`. User wants all pushable Pi customizations committed, but credentials/secrets must not be visible in the repo; README should document one-command install/restore. <!-- created=2026-06-11, last=2026-06-11 -->