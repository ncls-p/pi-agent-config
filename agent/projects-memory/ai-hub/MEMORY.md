AI Hub essentials (consolidated): UX like `../AltScribe/`: single team/app, hide workspace/multi-tenant ceremony, auto default workspace, central admin/account, simple personal/team/public marketplace, progressive disclosure, no legacy aliases/fallbacks. Use `python3`. Next.js 16 proxy/middleware: `src/proxy.ts` with next-intl proxy. Auth: centralize in `src/server/domain/services/authorization.ts`; require active `workspace_members`; `SYSTEM_ROLES` > stale DB perms; owner/admin workspace-wide; admin-created admins get `workspace.admin`; member APIs accept `workspace.admin`; custom-tool mutations require `tools.configure`, built-in listing uses `tools.view`. Chat/tools: merge status by `toolCallId`; resend uses `X-User-Message-Id`, excludes source, deletes `tool_invocations`; stop via `stopGeneration`/stop route/`stream-bus.ts`; queued messages send after current stream. Code workspace: ZIP/code upload, HTML/CSS/JS tree/editor/live preview, model edits/live changes, and ZIP redownload stay inside chat tool/artifact flow, not a dedicated page; Next route ZIP `Response` needs `Uint8Array` -> `ArrayBuffer`. Providers/agents/tasks: `ai_providers`; Dragonfly kind=`dragonfly`, base `/api/v1`, reasoning at `choices[].delta.reasoning_content`; sanitize Anthropic thinking tool calls before `@ai-sdk/openai-compatible`; built-ins in `src/modules/tool/builtin-tools.ts`; skills via `npx skills add`, stored in `agent_skills.markdown_files_json`, injected by `buildSkillsSystemPrompt`, managed at `/tools?tab=skills`; agents under `src/app/(workspace)/agents/...`, schema has `slug`, `agent_versions` sync advanced JSON/tool fields, max tokens `30_000` server and `"30000"` forms; scheduled tasks in `src/modules/scheduled-tasks/`, `/api/workspace/scheduled-tasks`, `src/components/chat/scheduled-task-manager.tsx`; worker enqueues due tasks as normal conversation messages. Marketplace 2.0: tables `marketplace_item_versions`/`marketplace_item_shares`, visibility public/private/unlisted/organization, no review gate, APIs `/api/marketplace/items`, UI `/marketplace`, installs agent/skill/custom_tool; Drizzle mocks use `mockReturnThis()`, avoid eslint-disable for pi-lens. Deploy/Coolify: prod `maiah.shiftify.eco`; validate/E2E-test changes; direct commit+push OK for prod hotfixes. Keep Dockerfile/prod/dev compose first-class with runner/worker/migrator targets. SearXNG image must patch effective engine metadata and CI verifies pushed image; compose depends on healthy searxng. Secrets: Better Auth >=32 chars, Dragonfly >=16, app/object encryption 64 hex, Garage key starts `GK` + 24+ alphanumeric. <!-- created=2026-06-10, last=2026-06-18 -->
§
AI Hub code workspace UX: uploaded code projects stay in-chat. Coding mode should use a compact left chat/composer plus main workbench/preview area; Chat/Coding toggle must work reliably, and historical artifact cards must not force Coding mode or duplicate preview mounts. <!-- created=2026-06-18, last=2026-06-18 -->
§
AI Hub code workspace UX: uploaded ZIP/code workspaces stay inside chat, not a dedicated page. Support Chat/Coding modes; Coding mode should show a compact chat/composer column on the left and a large workbench in the main area. The toggle must be user-controlled (don’t auto-bounce back to Coding when old artifacts render). Workbench needs syntax-colored code, a real directory tree, fullscreen preview/code, and preview refresh only on artifact/version changes—not noisy repeated GET loops. <!-- created=2026-06-18, last=2026-06-18 -->
§
AI Hub GitHub/repository integration requirement: each user has distinct repositories; repo connections, allowlists, GitHub installation/account mappings, and push/PR permissions must be scoped per user, not globally per workspace/org. <!-- created=2026-06-19, last=2026-06-19 -->
§
AI Hub GitHub publishing UX requirement: after model proposes GitHub publishing in chat, the user must choose whether to create a PR or push directly, and must be able to select the target repository and target branch, including main if they explicitly choose it; enforce this with explicit confirmation, policies, and audit logs. <!-- created=2026-06-19, last=2026-06-19 -->