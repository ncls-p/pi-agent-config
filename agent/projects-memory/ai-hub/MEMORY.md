ai-hub baseline/deploy: Vitest uses V8 coverage via vitest.config.mts; maintain >=95% coverage. PR previews deploy to `https://maiah-pr-{PR_NUMBER}.shiftify.eco`; healthy protected routes return HTTP 401 due Traefik basic auth. Coolify previews are memory-constrained: cap Dragonfly with DRAGONFLY_MAXMEMORY default 256mb. Sandbox deploy images are large: reuse host cache (`docker image inspect || docker pull`) and `main-latest` for app-only changes; rebuild/pull PR-specific sandbox images only when `opensandbox/**` or `scripts/build-opensandbox-image.sh` changes. <!-- created=2026-07-02, last=2026-07-02 -->
§
ai-hub Coolify/OpenSandbox deploys: `opensandbox/server` is only the control plane and launches runtime images via the host Docker socket; host must have `ai-hub-sandbox`, `opensandbox/execd`, and `opensandbox/egress`. Reuse cached images (`docker image inspect || docker pull`) and `main-latest` for app-only changes; rebuild/pull PR-specific sandbox images only when `opensandbox/**` or `scripts/build-opensandbox-image.sh` changes. <!-- created=2026-07-02, last=2026-07-02 -->
§
ai-hub sandbox/runtime: The project replaced OpenSandbox with a custom `sandbox-runner` architecture. The runner communicates via a Unix socket (`/run/sandbox/sandbox.sock`) and runs with no internet access, resource limits, and broad Python/Node/data libraries. The runner image (`ai-hub-sandbox-runner`) is significantly smaller (~2-3GB) than previous OpenSandbox images. Host dependencies like `opensandbox/server`, `execd`, and `egress` are no longer required for the new runner implementation. <!-- created=2026-07-02, last=2026-07-02 -->
§
AI Hub CI/CD workflow deploys PR previews to `https://maiah-pr-{PR_NUMBER}.shiftify.eco`; preview health/public routes are protected by Traefik basic auth and should return HTTP 401 when healthy. <!-- created=2026-07-02, last=2026-07-02 -->
§
ai-hub sandbox architecture: OpenSandbox was replaced by a custom `sandbox-runner` container/image target communicating over Unix socket `/run/sandbox/sandbox.sock`; it runs with no internet access, resource limits, and broad Python/Node document/data libraries. Local smoke validation showed `ai-hub-sandbox-runner:local` around 2.94GB vs prior OpenSandbox images >11GB. <!-- created=2026-07-02, last=2026-07-02 -->
§
ai-hub code workspace/tool gating: models must not enter code workspace mode or get code workspace tools unless those tools are explicitly enabled/bound. Do not auto-inject code workspace tools or text-to-workspace protocols when tools are disabled. <!-- created=2026-07-02, last=2026-07-02 -->
§
ai-hub accessibility convention: preserve WCAG/a11y guardrails such as programmatically labelled custom controls (Input/Textarea/SelectTrigger/Switch/Checkbox), visible focus styles/scroll margins, default Button type='button', and keyboard alternatives for drag-only interactions; `test/unit/accessibility-controls.test.ts` guards labelled controls. <!-- created=2026-07-02, last=2026-07-02 -->