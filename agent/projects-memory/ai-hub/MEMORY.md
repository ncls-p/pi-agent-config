ai-hub core conventions: Runtime merges tool-call/results by `toolCallId`; status priority error/rejected > approval pending > pending > completed; rejection sets `denied:true`. Chat: Streamdown animates last block; scroll uses `autoFollowRef` (wheel-up disables, near-bottom re-enables); resend uses `X-User-Message-Id`, excludes source, deletes `tool_invocations`; stop via `stopGeneration`, `/api/workspace/conversations/[conversationId]/stop`, `stream-bus.ts` AbortController; sync in `src/modules/chat/chat-sync.ts` BroadcastChannel with `cache:no-store`. Providers: config `ai_providers`; TempleCloud Dragonfly kind=`dragonfly`, base `/api/v1`, reasoning in `choices[].delta.reasoning_content`; sanitize Anthropic `delta.tool_calls:[{type:"thinking"}]` before `@ai-sdk/openai-compatible`. Agents/tools/skills: agent routes in `src/app/(workspace)/agents/...`; schema has `slug`; `agent_versions` sync toolChoice/responseFormatJson/memoryPolicyJson/guardrailsJson/approvalPolicyJson; max tokens style `30_000` server and `"30000"` forms; built-ins in `src/modules/tool/builtin-tools.ts`, rendered in `src/components/chat/chat-message-list.tsx`; `web_search` includes date; skills via `npx skills add`, stored in `agent_skills.markdown_files_json`, injected by `buildSkillsSystemPrompt`, managed at `/tools?tab=skills`. UX/UI: progressive disclosure, no workspace label, `AdvancedSection` closed by default, resizable sidebars persist in `localStorage`, compact `EN`/`FR` switcher, tool builder stays minimal (no n8n/MCP mentions, collect secrets first via chat card, auto-continue, real progress). Shell: use `python3` not `python`. <!-- created=2026-06-10, last=2026-06-11 -->
§
ai-hub Marketplace 2.0: schema includes `marketplace_item_versions`, `marketplace_item_shares` (user-to-user), visibility public/private/unlisted/organization, indexes `idx_marketplace_items_tenant`/`idx_marketplace_items_publisher`, migration `0013_marvelous_spiral_bnd.sql`; status has no review gate. API: POST/GET `/api/marketplace/items`, PATCH/DELETE `/[itemId]`, POST `/[itemId]/publish`, `/share`, `/feature`, `/moderate`; `/api/admin/users` returns `{ users: [...] }`. UI: `/marketplace/page.tsx` tabs Discover/My Items/Drafts/Shared and `/marketplace/items/[itemId]/page.tsx`; `installMarketplaceItem` handles agent/skill/custom_tool. Sharing UX: from resource/list actions (e.g. agents), Share first offers Marketplace vs User; Marketplace path has no draft/review flow, only light customization, then publish directly and immediately available. Tests in `test/unit/marketplace-use-cases.test.ts` (`test/**/*.test.ts`); Drizzle mocks use `mockReturnThis()`; pi-lens ignores eslint-disable so restructure code instead. <!-- created=2026-06-10, last=2026-06-11 -->
§
ai-hub light theme contrast fix applied (commit 48f733f): --muted-foreground darkened to oklch(0.40…) (~8:1 ratio), --border solidified to oklch(0.76…), --secondary/muted surfaces adjusted. Original --muted-foreground #636464 failed WCAG AA (~2.6:1). User rejected both too-light and too-dark extremes; balanced values settled. <!-- created=2026-06-10, last=2026-06-10 -->
§
ai-hub Marketplace sharing UX preference: from resource list items (especially agents), Share should first offer Marketplace vs User. Choosing Marketplace should allow light listing customization and publish immediately to marketplace; avoid a separate draft/review story in this flow. <!-- created=2026-06-11, last=2026-06-11 -->