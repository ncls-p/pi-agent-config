AltScribe verification/tooling: full regression/pre-deploy runs `npm run typecheck`, `npm run lint`, `npm test`, `npm run test:integration`, `npm run build`, and Playwright E2E (`npm run test:e2e` or `./node_modules/.bin/playwright test`). Strict lint: `npm run lint -- --max-warnings=0`; single file: `npx eslint <path>` (flat config has no `--file`). Next build may print PostgreSQL during page-data collection; fix `next-env.d.ts` EACCES with project-user ownership + 644. Audio preprocessing requires `ffmpeg`. Secret scanner can false-positive on i18n labels like `Password: "Mot de passe"`. <!-- created=2026-06-01, last=2026-06-02 -->
§
AltScribe live transcription: upload standalone ~5min browser audio chunks during meetings, transcribe immediately, then assemble at finish. Avoid MediaRecorder timeslice concatenation; later WebM fragments may lack EBML headers (`EBML header parsing failed`); stop/restart per segment. Live chunk upload failures must not discard the final recording blob: preserve full-audio fallback, enqueue `assemble-live-transcription` asynchronously, and let server assembly fall back to full recording transcription for recoverable chunk failures. <!-- created=2026-06-01, last=2026-06-02 -->
§
AltScribe Drizzle migrations: `npx drizzle-kit generate` reads `drizzle.config.ts` and writes SQL to `./drizzle/`. Check existing migrations first; if many schema changes were added at once, Drizzle may generate one large delta file that may need manual splitting for incremental migration. <!-- created=2026-06-01, last=2026-06-02 -->
§
AltScribe deployment/config: production runs in Coolify via `docker-compose.prod.yml`; object storage is S3-compatible Garage, audio keys live in `meetings.audioPath` / `meeting_audio_files.audioPath`, bucket defaults to `meetscribe-recordings` unless `ALTSCRIBE_OBJECT_STORAGE_BUCKET` is set. For SSH dev use `ssh -L 3000:localhost:3000 ncls@100.98.140.47`, browse `http://localhost:3000`, set `BETTER_AUTH_URL=http://localhost:3000`, and add localhost/LAN to `BETTER_AUTH_EXTRA_TRUSTED_ORIGINS` if needed. LAN/IP HTTP can trigger Better Auth origin 403s and browser audio capture failures because getUserMedia/MediaRecorder needs secure context; localhost counts as secure. SSO target is Microsoft Entra ID single-tenant, open to whole company. <!-- created=2026-06-01, last=2026-06-02 -->
§
AltScribe local dev over SSH tunnel: use `ssh -L 3000:localhost:3000 ncls@100.98.140.47`, browse `http://localhost:3000`, and set `BETTER_AUTH_URL=http://localhost:3000`; include localhost/LAN in `BETTER_AUTH_EXTRA_TRUSTED_ORIGINS` if needed. LAN/IP HTTP can cause Better Auth origin 403s and browser audio capture failures because getUserMedia/MediaRecorder requires a secure context; localhost is treated as secure. <!-- created=2026-06-02, last=2026-06-02 -->
§
AltScribe SSO target: Microsoft Entra ID single-tenant with access open to the whole company, not restricted to specific groups/users. <!-- created=2026-06-02, last=2026-06-02 -->
§
AltScribe Playwright quirk: `npx playwright install` may hang after 100% during extraction with partial ~18MB cache. Workaround for Chromium: manually unzip `/tmp/playwright-download-*/playwright-download-chromium-ubuntu24.04-x64-1217.zip` into `~/.cache/ms-playwright/chromium-1217`, chmod chrome executable, touch `INSTALLATION_COMPLETE`, then run `./node_modules/.bin/playwright test --project=chromium` from repo. <!-- created=2026-06-02, last=2026-06-02 -->
§
AltScribe Microsoft/Entra SSO uses Better Auth provider env vars `MICROSOFT_CLIENT_ID`, `MICROSOFT_TENANT_ID`, and `MICROSOFT_CLIENT_SECRET`; only the secret value goes in `MICROSOFT_CLIENT_SECRET` (not the Entra secret ID). SSO accounts should link to existing email/password users by matching email so the same account is reused. <!-- created=2026-06-08, last=2026-06-08 -->
§
AltScribe large audio uploads bypass proxy body limits by sending small chunks to `/api/meetings/:meetingId/upload/chunks` and finalizing via `/api/meetings/:meetingId/upload/complete`; this path is used for both manual audio upload and live-recording finish, then server-side reassembles parts through Garage/S3 and queues normal meeting processing. In prod, 20 MB and 4 MB chunks still hit 413 on `altscribe.deodis.com`, so prefer sub-1 MB chunks (512 KB was the next fix) unless proxy limits are raised. <!-- created=2026-06-09, last=2026-06-09 -->
§
AltScribe deployment workflow: committing and pushing to `origin/main` is expected to trigger production redeploy using `docker-compose.prod.yml`. <!-- created=2026-06-09, last=2026-06-09 -->