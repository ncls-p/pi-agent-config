AltScribe verification/tooling: pre-deploy regression runs `npm run typecheck`, `npm run lint`, `npm test`, `npm run test:integration`, `npm run build`, and Playwright E2E (`npm run test:e2e` or `./node_modules/.bin/playwright test`). Strict lint: `npm run lint -- --max-warnings=0`; single file: `npx eslint <path>` (flat config has no `--file`). Next build may print PostgreSQL during page-data collection; fix `next-env.d.ts` EACCES with project-user ownership + 644. Audio preprocessing requires `ffmpeg`; secret scanner can false-positive on i18n labels like `Password: "Mot de passe"`. Drizzle: `npx drizzle-kit generate` uses `drizzle.config.ts` and writes to `./drizzle/`; inspect/split large generated deltas if needed. Playwright install quirk: if Chromium install hangs after 100%, manually unzip cached archive into `~/.cache/ms-playwright/chromium-1217`, chmod chrome, touch `INSTALLATION_COMPLETE`, then run tests with `--project=chromium`. <!-- created=2026-06-01, last=2026-06-16 -->
§
AltScribe audio/transcription: live meetings upload standalone ~5min browser audio chunks and transcribe immediately, then assemble at finish. Avoid MediaRecorder timeslice concatenation because later WebM fragments may lack EBML headers; stop/restart per segment. Never discard final recording on live chunk failures: preserve full-audio fallback, enqueue `assemble-live-transcription` asynchronously, and let server assembly fall back to full-recording transcription for recoverable chunk failures. Large manual/live uploads use `/api/meetings/:meetingId/upload/chunks` then `/api/meetings/:meetingId/upload/complete`, reassemble via Garage/S3, and queue normal processing; use conservative 256 KB app-upload chunks unless proxy limits are raised. Dragonfly/STT uploads also need small chunks; default `ALTSCRIBE_STT_CHUNK_SECONDS` is 120s. <!-- created=2026-06-01, last=2026-06-16 -->
§
AltScribe deployment/config: production runs in Coolify via `docker-compose.prod.yml`; object storage is S3-compatible Garage, audio keys live in `meetings.audioPath` / `meeting_audio_files.audioPath`, bucket defaults to `meetscribe-recordings` unless `ALTSCRIBE_OBJECT_STORAGE_BUCKET` is set. For SSH dev use `ssh -L 3000:localhost:3000 ncls@100.98.140.47`, browse `http://localhost:3000`, set `BETTER_AUTH_URL=http://localhost:3000`, and add localhost/LAN to `BETTER_AUTH_EXTRA_TRUSTED_ORIGINS` if needed. LAN/IP HTTP can trigger Better Auth origin 403s and browser audio capture failures because getUserMedia/MediaRecorder needs secure context; localhost counts as secure. SSO target is Microsoft Entra ID single-tenant, open to whole company. <!-- created=2026-06-01, last=2026-06-02 -->
§
AltScribe Microsoft/Entra SSO uses Better Auth provider env vars `MICROSOFT_CLIENT_ID`, `MICROSOFT_TENANT_ID`, and `MICROSOFT_CLIENT_SECRET`; only the secret value goes in `MICROSOFT_CLIENT_SECRET` (not the Entra secret ID). SSO accounts should link to existing email/password users by matching email so the same account is reused. <!-- created=2026-06-08, last=2026-06-08 -->
§
AltScribe recap table support: editable recap UI is `components/meeting-recap-editor.tsx` using Tiptap Table extensions; Markdown table render/round-trip serialization lives in `lib/recap-rich-text.ts`; regression coverage is in `lib/recap-rich-text.test.ts` and recap/workspace E2E coverage in `e2e/altscribe.spec.ts`. <!-- created=2026-06-16, last=2026-06-16 -->
§
AltScribe recap editor tables: meeting recap editing uses TipTap table support plus custom `lib/recap-rich-text.ts` Markdown table ⇄ HTML table round-trip. To support tables, import named exports from `@tiptap/extension-table` (`{ Table, TableCell, TableHeader, TableRow }`); the package has no default `Table` export. Table UI controls live in `components/meeting-recap-editor.tsx` and E2E coverage in `e2e/altscribe.spec.ts`. <!-- created=2026-06-16, last=2026-06-16 -->
§
AltScribe codebase analysis: Next.js 16 App Router, React 19, Better Auth, Postgres + Drizzle, S3-compatible object storage (Garage), AI SDK recap/chat, STT providers, shadcn/ui, Streamdown, Vitest, Playwright. Core domain is meetings/transcripts/recaps/chat. Server-side layered: app/service → domain models → postgres repositories/infrastructure adapters. Auth supports email/password + optional Microsoft SSO, admin-only login/register gating, rate limiting, encrypted sensitive meeting content at rest via ALTSCRIBE_ENCRYPTION_KEY. Testing: typecheck clean, lint clean, 30 Vitest files pass. <!-- created=2026-06-17, last=2026-06-17 -->