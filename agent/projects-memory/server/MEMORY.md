llama-service/scripts/llama-queue-proxy.py proxies OpenWebUI to llama.cpp. llama.cpp chat/completions accepts only text/image_url/input_audio content parts; normalize input_text->text, input_image/Anthropic image->image_url, raw string parts->text, drop malformed/unsupported parts, and reset Content-Length. `unsupported content[].type` persisted until raw/non-object content[] parts were handled; later no-response/500 was CUDA OOM or a dead model port after OOM, resolved by restarting llama-server.service. <!-- created=2026-06-06, last=2026-06-06 -->
§
llama-service proxy debugging lesson: if OpenWebUI image upload still returns `unsupported content[].type`, check that `llama-queue-proxy.py` actually reassigns `msg["content"]` after normalizing parts, not only when dropping parts; restart `llama-queue-proxy.service` and inspect journal logs. <!-- created=2026-06-06, last=2026-06-06 -->
§
llama-service/OpenWebUI vision debugging: after `unsupported content[].type` is fixed, `no response profile`, `0.00 T/s`, or HTTP 500 can be a llama.cpp backend crash/CUDA OOM during image processing. Check `journalctl --user -u llama-server.service`; logs may show prompt processing tens of thousands of tokens then `CUDA error: out of memory` / failed connection. <!-- created=2026-06-06, last=2026-06-06 -->
§
video-service now supports image-to-video for model `ltx-2.3-22b-distilled-q5km`: `/v1/videos/generations` accepts `image`, `image_url`, `input_image`, or `first_frame_image` as data URL/base64/URL/local path/object, plus optional `image_strength` (0-1) and `image_compression` (0-100). It builds ComfyUI nodes `LoadImage -> LTXVPreprocess -> LTXVImgToVideoInplace` and is routed through the global proxy on port 8081. <!-- created=2026-06-06, last=2026-06-06 -->
§
music-service supports ACE-Step music modification: `/v1/music/edits` and `/v1/music/generations` accept source audio via `source_audio`/`src_audio`/`input_audio` as server path, http(s) URL, data URL, or base64. `task_type` auto-selects `cover` for source audio and `repaint` for source audio plus repaint range; explicit task types include `cover`, `repaint`, `lego`, `extract`, `complete`. Edit fields include `reference_audio`, `repainting_start/end`, `audio_cover_strength`, `cover_noise_strength`, `repaint_mode/strength`, `retake_seed/variance`. Proxy routes `/v1/music/*` to music-service. <!-- created=2026-06-06, last=2026-06-06 -->
§
Server install convention: avoid Docker for local AI services when possible; prefer uv/local virtualenv installs. User explicitly requested this during Unsloth Studio setup. <!-- created=2026-06-13, last=2026-06-13 -->
§
Unsloth Studio is intentionally kept stock on this server: installed under `/home/ncls/.unsloth/studio` with no `UNSLOTH_STUDIO_HOME`, `HF_HUB_CACHE`, `LLAMA_CACHE`, `LLAMA_SERVER_PATH`, or `UNSLOTH_LLAMA_CPP_PATH` service overrides. UI is direct on port 8888; global proxy 8081 remains for llama/image/video/music only. <!-- created=2026-06-13, last=2026-06-13 -->