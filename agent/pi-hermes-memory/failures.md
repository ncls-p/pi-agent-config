[failure] ai-hub: When user rejects a tool, the server still sends a `tool_result` that merges into `tool-call`, making `output` defined → `getToolStatus` returns "completed" (green). Fix: mark `tool-call` with `denied: true` in `resolveApproval` on reject, and check `denied` before `output` in status logic. Also move `toolNameMatches` to `chat-types.ts` so both the stream hook and UI can match the rejected part. <!-- created=2026-06-01, last=2026-06-01 -->
§
Design-enhancer/Pi TUI preferences: make completed read operations visually prominent like edit/write (not dim-only for '✓ read 103 lines'); ellipsis/truncation preview lines must keep colored styling; context-mode tool output should be compact/distinct ('context · <kind>' with concise meta and capped colored preview), not raw dumps; implement Pi UI/design changes through extensions (e.g. design-enhancer), not by editing installed @earendil-works/pi-coding-agent core/dist files. <!-- created=2026-06-07, last=2026-06-07 -->
§
[tool-quirk] Pi TUI tool renderer quirk: for live/streaming write previews, rendering content in write.renderResult is too late; during model argument streaming only write.renderCall updates (args.content grows). To show live file content when expanded, render args.content in renderCall; keep collapsed compact and final content in renderResult. — Failed: Initial design-enhancer change put the preview in renderResult, so the user only saw the line count increase during writing and the full file appeared only after completion. <!-- created=2026-06-07, last=2026-06-07 -->
§
[correction] When the user says “l’accueil de pi” or points to the `π pi / coding agent` screen, they mean Pi’s startup/welcome menu UI, not an app project homepage such as ai-hub. Do not jump to editing ai-hub home pages; inspect/modify Pi TUI startup/welcome rendering instead. — Failed: Assistant initially interpreted “Améliore le design de l'acceuil” as ai-hub homepage and inspected ai-hub files; user corrected that they meant the Pi welcome screen. <!-- created=2026-06-07, last=2026-06-07 -->
§
[preference] Pi TUI design preference: do not reduce or shorten file paths in displayed tool/menu chrome; show full paths rather than `…/filename` or compacted segments. — Failed: User explicitly asked to remove the path-size reduction in display after noticing shortened paths in the design-enhancer extension. <!-- created=2026-06-07, last=2026-06-07 -->
§
[tool-quirk] Next.js dynamic import pattern `import("next/server").NextResponse` fails at compile time because NextResponse is not a property of the module promise — it's a named export. Use static import `import { NextResponse } from "next/server"` instead. LSP error: "Property 'NextResponse' does not exist on type 'Promise'". <!-- created=2026-06-07, last=2026-06-07 -->
§
[insight] In ai-hub, `npm run dev` first runs `tsx scripts/migrate.ts`; if it fails with `Cannot find module './schema'` from `src/server/infrastructure/db/index.ts`, check whether `src/server/infrastructure/db/schema.ts` was deleted and whether `migrations/meta/_journal.json` references a missing SQL migration. — Failed: A deleted tracked Drizzle schema file caused runtime module resolution failure before Next dev started. <!-- created=2026-06-08, last=2026-06-08 -->
§
In ai-hub chat automation, AI SDK generateObject failed for some configured provider/model combinations with "No object generated: the model did not return a response." Prefer generateText with strict JSON prompt + tolerant parser for metadata title/suggestion generation, plus local fallback suggestions if parsing/generation fails. <!-- created=2026-06-08, last=2026-06-08 -->
§
[failure] In ai-hub, the `__drizzle_migrations` tracking table does NOT exist because earlier migrations (0000–0007) were applied manually (not via `drizzle-kit migrate` or `drizzle-orm/node-postgres/migrator`). This means `scripts/migrate.ts` (which calls `drizzle-orm/node-postgres/migrator`) silently does nothing or fails when new migration files are added — the migrator can't track state without `__drizzle_migrations`. Solution: use `drizzle-kit push` to apply the schema directly to the DB, then manually sync the migration SQL file + `_journal.json` entry for production deployments. <!-- created=2026-06-09, last=2026-06-09 -->
§
[correction] Correction for ai-hub skills: full skill Markdown should not be appended to every prompt. Use metadata discovery plus on-demand skill loading/tool invocation instead. — Failed: The user explicitly asked to verify web conventions and ensure skills are called/loaded by the agent, not merely added to the prompt each time. Existing memory/implementation indicated selected skills were injected into chat system prompts, which conflicts with the standard progressive-disclosure convention. <!-- created=2026-06-09, last=2026-06-09 -->
§
[failure] ai-hub modal responsive failure: desktop-centered fixed-height skill view/edit dialogs did not work on mobile. Use fullscreen mobile layouts with fixed header/footer and only inner content scrolling; use horizontal file rail on mobile and sidebar on desktop. — Failed: Initial skill view/edit modals used a centered desktop dialog with fixed viewport heights and mobile compromises (including a select for files), which the user said was not responsive at all. The fix was a mobile-first fullscreen modal (`h-dvh`/`w-screen`), non-scrolling header/footer, rail/sidebar file navigation, and a single scrollable content/editor pane. <!-- created=2026-06-09, last=2026-06-09 -->
§
[tool-quirk] Drizzle ORM TypeScript inference breaks when building queries dynamically by reassigning a variable (e.g., let query = db.select(); query = query.where(...)). Fix: use `sql` template literal for dynamic conditions (`where(conditions.length ? sql`${and(...conditions)}` : undefined)`) or build the entire chain in a single expression. <!-- created=2026-06-09, last=2026-06-09 -->
§
[tool-quirk] When mocking Drizzle ORM chains with vi.mock: mock factories are hoisted to top of file, so mock chain objects must be created INSIDE the factory. Expose chains via `dbModule._selectChain` etc. for test configuration. Use `mockReturnThis()` for chain methods, but `.where().limit()` or `.where().returning()` must return the final value (not chain). For `.where().limit()`, make `.where()` return an object with `.limit()` that resolves. <!-- created=2026-06-09, last=2026-06-09 -->
§
[failure] Tried using .filter() on the result of /api/admin/users, which caused a Runtime TypeError because the endpoint returns an object { users: [...] } instead of a plain array. Fixed by using: Array.isArray(data) ? data : data.users ?? []. — Failed: Response object wrapping array <!-- created=2026-06-10, last=2026-06-10 -->
§
[tool-quirk] ai-hub test files: `vi.mock("@/server/infrastructure/db", ...)` combined with `declare module "@/server/infrastructure/db"` causes TypeScript errors (2451: Cannot redeclare block-scoped variable, ReturnType used as value). Fix: remove `declare module` augmentation entirely. Instead, define a standalone `DbModule` interface and cast the import: `const dbModule = _dbModule as unknown as DbModule`. Expose mock chains via `_c`, `_sc`, etc. properties on the mock return object. For `makeChain()`-style helpers called inside hoisted `vi.mock` factories, move CHAIN_KEYS or equivalent constants outside the factory or use `let` with lazy initialization to avoid TDZ errors. <!-- created=2026-06-10, last=2026-06-10 -->
§
[correction] ai-hub provider use-cases: `createModel(providerId, input)` takes 2 args, but `CreateModelInput` type ALSO requires `providerId` inside the input object (redundant). Correct call: `createModel("prov-1", { providerId: "prov-1", modelId: "gpt-4" })`. Similarly, `createProvider` expects `kind` to be `"openai-compatible"` not `"openai"`. <!-- created=2026-06-10, last=2026-06-10 -->
§
[tool-quirk] `edit` tool may fail on UTF-8 multi-byte chars (e.g., curly quotes in locale JSON). After retries fail, use `sed` instead. See ai-hub session: adding `marketplace.share.publish` to `messages/en.json` failed twice via `edit`; `sed -i '/"action": "Share",/a\      "publish": "Publish",'` worked. <!-- created=2026-06-10, last=2026-06-10 -->
§
[tool-quirk] ai-hub/tool quirks: use `python3` or Node scripts because `python` may be missing; shell grep can miss Unicode emojis, so use Node with Unicode-aware regex ranges; pi-lens custom rules ignore `eslint-disable`, so restructure code to satisfy diagnostics instead of suppressing. — Failed: Consolidates duplicate tool-quirk memories for ai-hub shell/Pi diagnostics. <!-- created=2026-06-11, last=2026-06-11 -->
§
[convention] ai-hub conventions/risks: tests live in `test/` (`test/unit/`, `test/e2e/`); run `npm run test:ci`. Known risks: in-memory worker queue at `src/server/infrastructure/worker/index.ts` (jobs lost on restart; consider Redis/Dragonfly), hardcoded dev IPs in `src/lib/auth.ts` `developmentOrigins`, many silent catches returning null/undefined/[], and some API routes missing Zod validation. — Failed: Consolidated project-specific testing convention and security/reliability risks. <!-- created=2026-06-11, last=2026-06-11 -->
§
[correction] User corrected a git backup task: after asking to run GitHub repo init commands for pi-agent-config, only README.md was committed/pushed while actual Pi custom files remained untracked. When the user asks to back up/push configs/customs, include all safe pushable custom files, not just the README/bootstrap commands; inspect untracked files and exclude secrets/caches before committing. — Failed: The requested commands were executed literally, creating/pushing only README.md, which did not satisfy the underlying request to save Pi customs. What worked instead: check git status/untracked files and commit all safe custom config files while excluding secrets/caches. <!-- created=2026-06-11, last=2026-06-11 -->